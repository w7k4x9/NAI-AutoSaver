# NAI 자동저장 v1.0.27

NovelAI 이미지 생성 페이지(`https://novelai.net/image`)에서 사용하는 Chrome Manifest V3 확장 프로그램입니다.
자동 생성, 이미지 자동 저장, 대기열, 조합 생성, Base Prompt와 캐릭터 프롬프트 분리 입력을 지원합니다.

## 주요 기능

- **자동 생성**: 지정한 횟수와 주기에 따라 NovelAI의 Generate 버튼을 자동 실행
- **이미지 자동 저장**: 생성이 완료될 때마다 결과 이미지를 자동 다운로드
- **저장 이름 관리**: `1_b_c`를 입력하면 `1_b_c (1).png`, `1_b_c (2).png`처럼 순번을 붙여 저장
- **대기열**: 항목마다 저장 이름, Base Prompt, Base Undesired Content, 캐릭터 프롬프트, 캐릭터 Undesired Content, 생성 횟수를 따로 설정
- **프롬프트 메모**: 베이스 프롬프트·베이스 네거티브·캐릭터 태그·캐릭터 네거티브를 1개 이상 선택해 저장하고, 대기열 스타일 카드와 작업 드롭다운으로 관리
- **다중 캐릭터**: `;;`로 구분한 캐릭터를 NovelAI의 Character 1, Character 2… 카드에 순서대로 입력
- **캐릭터 카드 자동 생성**: 필요한 수만큼 Add Character를 누르고 Female, Male, Other를 자동 선택
- **조합 생성기**: 캐릭터, 표정, 배경 조합을 자동으로 대기열 항목으로 생성
- **대기열 가져오기/내보내기**: JSON 형식 지원
- **글래스 UI**: 이동, 접기/펴기, 실행 상태 표시 지원

## 설치 및 업데이트

1. ZIP 파일의 압축을 풉니다.
2. Chrome 주소창에 `chrome://extensions`를 입력합니다.
3. 오른쪽 위의 **개발자 모드**를 켭니다.
4. 기존 버전이 켜져 있다면 끄거나 삭제합니다.
5. **압축해제된 확장 프로그램을 로드**를 누릅니다.
6. 압축을 푼 `NAI-AutoSaver-main` 폴더를 선택합니다.
7. 확장 프로그램 화면에서 새로고침 버튼을 누른 뒤, NovelAI 이미지 페이지도 새로고침합니다.

> 코드만 교체하고 NovelAI 페이지를 새로고침하지 않으면 이전 content script가 계속 작동할 수 있습니다.

## 기본 사용법

### 자동 생성

1. NovelAI 이미지 페이지를 엽니다.
2. 오른쪽 아래의 NAI 자동저장 패널을 펼칩니다.
3. 저장 이름, 프롬프트, 횟수, 주기를 입력합니다.
4. **자동 생성 시작**을 누릅니다.

`횟수`가 `0`이면 중지할 때까지 계속 생성합니다.

### Base Prompt와 캐릭터 프롬프트

Base Prompt와 캐릭터 프롬프트는 서로 다른 입력칸입니다.

- 장면, 화풍, 품질, 배경 등 공통 태그 → **Base Prompt**
- 인물별 성별, 머리색, 눈색, 의상 등 → **캐릭터 프롬프트**

캐릭터 프롬프트를 Base Prompt에 직접 붙일 필요는 없습니다. 확장 프로그램이 각 Character 카드에 따로 입력합니다.

## 다중 캐릭터 입력법

### 한 명

```text
1girl, black hair, red eyes
```

### 두 명

```text
1girl, black hair, red eyes
;;
1boy, white hair, blue eyes
```

### 세 명

```text
1girl, black hair, red eyes
;;
1boy, white hair, blue eyes
;;
1other, silver hair, green eyes
```

### 구분 규칙

- **한 캐릭터 안의 태그 구분**: 쉼표 `,`
- **캐릭터와 캐릭터 구분**: 쌍세미콜론 `;;`
- 줄바꿈은 선택 사항입니다.
- 화살표 `→`는 입력하지 않습니다.
- 세로줄 `|`도 캐릭터 구분자로 사용하지 않습니다.
- 전각 구분자 `；；`도 인식하지만 일반 `;;` 사용을 권장합니다.

한 줄로 작성해도 됩니다.

```text
1girl, black hair, red eyes ;; 1boy, white hair, blue eyes
```

### 성별 자동 선택

각 `;;` 블록의 성별 태그를 읽어 캐릭터 카드의 유형을 선택합니다.

- `1girl`, `girl`, `female`, `woman` → Female
- `1boy`, `boy`, `male`, `man` → Male
- 성별을 판단할 수 없는 경우 → Other

NovelAI는 Female/Male 카드 생성 후 `1girl` 또는 `1boy`를 화면에서 `girl,` 또는 `boy,`로 정규화해 표시할 수 있습니다. 이는 정상 동작입니다.

캐릭터 카드의 성별이 입력 블록과 다르면, v1.0.18은 첫 불일치 카드부터 뒤쪽 카드를 정리한 뒤 올바른 성별로 다시 생성합니다.

## 캐릭터 Undesired Content

캐릭터 네거티브도 긍정 프롬프트와 같은 순서로 `;;`를 사용합니다.

```text
bad hands, extra fingers
;;
long hair, red eyes
```

- 첫 번째 블록 → Character 1의 Undesired Content
- 두 번째 블록 → Character 2의 Undesired Content
- 비워두면 기존 값을 유지합니다.

## 프롬프트 메모

패널 왼쪽의 **메모** 탭에서 다음 4개 항목을 저장할 수 있습니다.

- 베이스 프롬프트
- 베이스 네거티브
- 캐릭터 태그
- 캐릭터 네거티브

저장할 항목만 체크합니다. 하나만 체크하면 개별 메모, 네 개를 모두 체크하면 한 세트로 저장됩니다.

- **NAI 현재값 읽기**: NovelAI 페이지에 현재 입력된 4종 값을 메모 편집기로 가져옵니다.
- **자동생성 값 가져오기**: 자동생성 탭의 4종 입력값을 가져옵니다.
- **입력칸 불러오기**: 메모에 포함된 항목만 자동생성 탭에 채웁니다. 포함되지 않은 항목은 유지됩니다.
- **NAI 직접 적용**: 메모에 포함된 항목만 NovelAI 입력 영역에 바로 적용합니다.
- **수정 / 삭제**: 저장된 메모를 편집하거나 제거합니다.

메모는 `chrome.storage.local`에 저장되므로 확장 프로그램을 닫거나 브라우저를 다시 시작해도 유지됩니다. 확장 프로그램을 삭제하면 함께 삭제될 수 있습니다.

## 대기열

1. **현재 프롬프트 추가**를 눌러 현재 설정을 대기열 항목으로 저장합니다.
2. 각 항목에서 다음 값을 개별 편집합니다.
   - 저장 이름
   - Base Prompt
   - Base Undesired Content
   - 캐릭터 프롬프트
   - 캐릭터 Undesired Content
   - 횟수
3. **대기열 실행**을 누르면 항목별 프롬프트를 적용하고 순서대로 생성·저장합니다.
4. 반복을 켜면 마지막 항목 이후 다시 첫 항목부터 실행합니다.

대기열의 저장 이름은 각 항목에 설정된 값이 사용됩니다. 자동 생성 화면의 단발 저장 이름과는 별개입니다.

## 조합 생성

Base Prompt 1개와 캐릭터·표정·배경 세트를 조합해 대기열을 자동 생성합니다.

항목 이름 형식:

```text
캐릭터코드_배경코드_감정번호
```

예시:

```text
w_h_1
```

캐릭터 프롬프트는 외형, 표정, 배경 태그를 합쳐 생성되며, 그룹 사이의 태그가 붙지 않도록 필요한 쉼표를 자동 보완합니다.

## 저장 위치

기본 저장 폴더는 브라우저 다운로드 폴더 아래의 `NovelAI`입니다.
설정의 **저장 폴더**에서 하위 경로를 변경할 수 있습니다.

예시:

```text
NovelAI/프로젝트A
```

같은 파일명이 이미 있으면 Chrome이 번호를 자동으로 덧붙입니다.

## 중지 방법

- 정상 중지: **자동 생성 중지** 또는 **대기열 중지**
- 생성 중 한 번 누르면 현재 생성 중인 이미지까지 저장한 뒤 멈추는 **정지 대기 중** 상태가 됩니다.
- 즉시 중지하려면 중지 버튼을 한 번 더 누릅니다.
- 패널이 접혀 있으면 빨간 정지 표시를 눌러 패널을 펼친 뒤 중지 버튼을 누릅니다.

## 문제 해결

### 수정본을 설치했는데 예전 동작이 계속됨

1. 기존 확장 프로그램을 끄거나 삭제합니다.
2. 새 폴더를 다시 로드합니다.
3. 확장 프로그램을 새로고침합니다.
4. NovelAI 페이지를 새로고침합니다.

여러 버전을 동시에 켜두면 두 content script가 함께 실행될 수 있습니다.

### 잘못된 캐릭터 카드가 남아 있음

이전 버전이 만든 Character 3, Character 4 등의 불필요한 카드는 휴지통 버튼으로 삭제한 뒤 다시 실행하는 것이 가장 확실합니다. v1.0.18도 현재 `;;` 블록 수와 성별에 맞춰 카드를 정리하지만, NovelAI의 UI 상태가 오래 남아 있으면 페이지 새로고침이 필요할 수 있습니다.

### “캐릭터 카드는 생성됐지만 Character N 영역을 찾지 못했습니다”가 표시됨

- NovelAI 이미지 페이지를 새로고침합니다.
- 다른 버전의 NAI 자동저장이 동시에 켜져 있지 않은지 확인합니다.
- Character Prompts 영역이 화면에 생성된 뒤 다시 실행합니다.
- 그래도 반복되면 현재 NovelAI UI 구조가 변경된 경우일 수 있습니다.

### 캐릭터 프롬프트가 Base Prompt에 들어감

v1.0.16 이전 수정 과정에서 발생했던 문제입니다. Base Prompt 끝에 남은 `| 1girl... | 1boy...` 부분을 직접 지우고 v1.0.18로 다시 실행합니다. 현재 버전은 캐릭터 영역을 찾지 못할 경우 Base Prompt로 병합하지 않고 오류를 표시합니다.

## 검증 명령어

```bash
node --check queue-store.js
node --check content.js
node --check background.js
node --check popup/popup.js
node tests/queue-store.test.js
```

## 변경 이력

### v1.0.27

- 메모 카드를 대기열 카드와 같은 순번·제목·미리보기 중심 레이아웃으로 정리
- 메모 카드의 5개 개별 버튼을 `대기열에 추가` 버튼과 `불러오기 · 관리` 드롭다운으로 통합
- 드롭다운 안에서 자동생성 입력칸 불러오기, NovelAI 직접 적용, 수정, 삭제를 그룹별로 제공
- 대기열 편집기의 베이스 네거티브와 캐릭터 네거티브가 입력 즉시 해당 항목에 저장되지 않던 문제 수정
- 다른 대기열 항목을 선택했다 돌아왔을 때 처음 캡처한 NovelAI 네거티브 값으로 되돌아가던 문제 수정

### v1.0.26

- 대기열 시작이 `대기열 중지` 상태에서 오래 멈추던 Character 선택 대기 로직 제거
- 실제 Character 헤더 휴지통 아이콘(`sc-7d0727b8-17`)만 삭제 버튼으로 사용
- 프롬프트/Position 보조 아이콘(`sc-7d0727b8-33`) 및 이미지 업로드 버튼 클릭 금지
- `4 → 2` 전환 시 마지막 Character 카드부터 직접 삭제 후 개수 확인
- 대기열 항목 시작 시 `프롬프트 적용 중` 상태 표시

### v1.0.25

- 대기열 시작 직후 Character 삭제 버튼을 찾지 못해 즉시 중단되던 문제를 수정했습니다.
- compact Character 카드의 편집기가 보여도 선택된 카드로 오판하지 않고, 해당 카드를 실제로 선택한 뒤 상단 삭제 버튼을 확인합니다.
- `sc-7d0727b8-33`을 사용하는 Position 아이콘과 Character 삭제 아이콘을 DOM 구조로 구분합니다.
- 매 항목마다 전체 Character를 초기화하지 않고 필요한 개수 차이만 조정합니다. (`4 → 2`는 뒤의 두 카드만 삭제)
- 대상 Character가 아닌 다른 카드의 삭제 버튼이나 이미지 업로드 버튼으로 폴백하지 않습니다.

### v1.0.24

- Character 삭제 버튼 탐색을 `.character-prompt-input` 내부로 제한했습니다.
- 공통 styled-components 버튼 클래스만으로 삭제 버튼을 판단하지 않습니다.
- 현재 NovelAI Character 삭제 아이콘(`sc-7d0727b8-33`)을 확인한 경우에만 라벨 없는 버튼을 클릭합니다.
- Base Image, Vibe Transfer, Precise Reference 등의 파일 업로드 버튼이 열리는 문제를 수정했습니다.
- 삭제 버튼을 확실히 식별하지 못하면 다른 버튼을 추측 클릭하지 않고 대기열을 중단합니다.

### v1.0.23

- 대기열 항목 전환 시 Character 카드 삭제 결과를 실제 카드 수로 검증합니다.
- 선택된 Character의 삭제 버튼이 카드 밖(portal)에 렌더링되어도 사용자 제공 구조로 탐색합니다.
- 삭제 확인창이 나타나는 NovelAI UI도 처리합니다.
- Character 재구성 실패 시 이전 프롬프트로 생성하지 않고 대기열을 중단합니다.
- 항목 전환 직후 NovelAI UI가 안정화될 시간을 추가했습니다.

### v1.0.22

- 저장된 프롬프트 메모 카드에 `대기열에 추가` 버튼을 추가했습니다.
- 메모를 대기열에 추가하면 대기열 제목으로 메모 이름을 그대로 사용합니다.
- 메모에 선택 저장된 프롬프트만 대기열 필드에 복사하고, 생성 장수는 현재 자동생성 장수 설정을 사용합니다.

### v1.0.21

- 대기열 캐릭터 프롬프트 적용 전에 기존 Character 카드를 전부 삭제하고 새 `;;` 블록 수만큼 다시 생성합니다.
- 접힌 캐릭터 카드를 먼저 활성화한 뒤 삭제 버튼을 찾도록 수정했습니다.
- NovelAI의 라벨 없는 단일 아이콘 삭제 버튼 구조(`display:flex; flex-direction:row; gap:10px`)를 인식합니다.

### v1.0.20

- 베이스 프롬프트, 베이스 네거티브, 캐릭터 태그, 캐릭터 네거티브를 선택 저장하는 프롬프트 메모 탭 추가
- 4개 중 1개 이상을 개별 메모로 저장하거나 4개 전체를 세트로 저장 가능
- NovelAI 현재값 및 자동생성 입력값을 메모 편집기로 가져오는 기능 추가
- 저장 메모를 자동생성 입력칸으로 불러오거나 NovelAI에 직접 적용하는 기능 추가
- 메모 수정 및 삭제, 브라우저 재시작 후 유지되는 로컬 저장 지원

### v1.0.19

- 시작음·완료음을 NovelAI 페이지가 아닌 Chrome 확장 전용 오프스크린 문서에서 재생하도록 변경
- 브라우저 자동재생 정책에 막힌 재생 오류를 조용히 무시해 시작·종료음이 모두 나지 않던 문제 수정
- 잘못 저장된 볼륨 값은 기본값 50%로 보정

### v1.0.18

- 비활성 캐릭터 카드를 실제 마우스 클릭처럼 선택한 뒤 편집기를 열도록 수정
- `♀ Female`, `♂ Male`, `⚧ Other`처럼 성별 기호가 포함된 메뉴 항목 인식 보강
- `;;` 블록의 성별과 기존 카드 성별이 다르면 불일치 카드부터 뒤쪽을 삭제하고 올바르게 재생성
- Character 2가 compact 상태일 때 `girl,`만 남고 두 번째 캐릭터 태그가 입력되지 않던 문제 수정

### v1.0.17

- Character Prompts 영역이 Base Prompt 영역 바깥에 렌더링되는 현재 NovelAI 구조 반영
- 문서 전체에서 Character N 헤더를 찾되 확장 프로그램 패널은 제외하도록 수정
- 성별 기호의 variation selector 때문에 Character 헤더를 놓치는 문제 보정
- 카드가 있는데도 Character N 영역을 찾지 못했다고 표시되는 문제 수정

### v1.0.16

- 캐릭터 프롬프트가 Base Prompt에 `|` 형태로 들어가던 잘못된 fallback 제거
- 중첩된 Character N 제목 인식 보강
- 캐릭터 카드 내부 편집기를 카드 범위 안에서만 찾도록 수정
- 캐릭터 영역 탐색 실패 시 Base Prompt에 병합하지 않고 오류 표시

### v1.0.15

- 접힌 캐릭터 카드를 누락해 Character 2, 3, 4가 연속 생성되던 문제 수정
- Character N 헤더를 기준으로 카드 수 계산
- 입력 전 캐릭터 카드를 자동으로 펼치고 Prompt 탭 활성화
- `1girl`과 `girl`, `1boy`와 `boy`를 같은 성별 태그로 판정
- 초과 생성된 캐릭터 카드를 `;;` 블록 수에 맞춰 정리

### v1.0.14

- 성별 선택 후 자동 생성된 성별 태그만 남고 뒤 태그가 누락되던 문제 수정
- 쉼표 단위로 태그를 실제 입력 이벤트처럼 순차 입력
- 입력 완료 후 전체 태그를 검사하고 누락 시 재시도

### v1.0.13

- Add Character 클릭 후 Female, Male, Other 메뉴가 나타나는 현재 UI 대응
- 캐릭터 태그를 읽어 성별 메뉴 자동 선택
- 텍스트 없는 아이콘형 Add Character 버튼 탐색 보강
- 성별 선택 후 실제 카드가 생성될 때까지 대기

### v1.0.12

- 다중 캐릭터 `;;` 분리 지원 보강
- Add Character 버튼을 text, aria-label, title, tooltip 기준으로 탐색
- 캐릭터 카드 생성 완료 후 다음 캐릭터 입력
- 전각 `；；` 지원
- UI의 잘못된 `|` 안내를 `;;`로 통일

### v1.0.11

- 캐릭터 Prompt와 Undesired Content를 탭 전환 방식으로 읽고 쓰도록 수정
- 캐릭터 긍정 프롬프트와 네거티브가 섞이는 문제 수정
- Base Undesired Content 읽기 보강
- 조합 생성의 외형, 표정, 배경 태그 끝에 쉼표 자동 보완

### v1.0.10

- NovelAI 캐릭터 프롬프트 DOM 클래스 변경 대응
- 캐릭터 네거티브 적용 및 대기열 변수 밀림 문제 수정
- Base Prompt 탐색이 캐릭터 편집기를 선택하지 않도록 방어 로직 추가
- 조합 생성에 배경 네거티브 입력칸 추가
